---
import type { SectionSection } from "src/types/payload-types";
import BlockRouter from "../BlockRouter.astro";
interface Props {
    section: SectionSection
}
const { section } = Astro.props;
---
<section class="section __section">
    <BlockRouter blocks={section.blocks || []} />
</section>